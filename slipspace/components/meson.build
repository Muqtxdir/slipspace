componentsdir = moduledir / 'components'

install_data(
  [
    '__init__.py',
  ],
  install_dir: componentsdir,
)

subdir('battery')
subdir('datetime')
subdir('microphone')
subdir('network')
subdir('speaker')
subdir('panel')
